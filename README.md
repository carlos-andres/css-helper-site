# css-helper-site
CSS Helper shows only the author CSS that actually won the cascade for any element you right-click. Declarations render exactly as written in your source files, with filenames on the right. No shorthand expansion, no UA defaults, no inherited chain — just what your CSS did.   Supports @layer, @media, @supports, inline styles.
